import './Botao.css'
import { useState } from 'react';

// export const Botao = ({ tipo, valor, clicked }) => {


//     if (clicked && { valor } === "0") {
//         return (<button className={tipo + 'zero'} value={valor}>{valor}</button>)
//     } else {
//         return (<button className={tipo} value={valor}>{valor}</button>)
//     }
// }
